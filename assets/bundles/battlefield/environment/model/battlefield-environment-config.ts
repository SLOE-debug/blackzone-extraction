/** 战场无限环境窗口的确定性分块参数。 */
export const BATTLEFIELD_ENVIRONMENT_WORLD_CONFIG = Object.freeze({
  seed: 0x6e71c39,
  chunkSize: 45,
  activeChunkRadius: 2,
  obstacleCellSize: 8,
  playerSafeRadius: 7.5,
});
