/** 主角稳定的形态与基础步态参数。 */
export const VANGUARD_CONFIG = Object.freeze({
  statureScale: 1,
  shoulderScale: 1,
  limbScale: 1,
  armorScale: 1,
  weaponScale: 1,
  walkSpeed: 1.05,
  strideDistance: 1.18,
});
